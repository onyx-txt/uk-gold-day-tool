# UK Gold Day Dashboard – Python dependencies
# Install with:  pip install -r requirements.txt

streamlit>=1.35.0   # the web app framework
requests>=2.31.0    # HTTP calls to the Open-Meteo API
pandas>=2.0.0       # data manipulation for the forecast tables
